import { ValidateCreateTodoPipe } from './validate-create-todo.pipe';

describe('ValidateCreateTodoPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateTodoPipe()).toBeDefined();
  });
});
